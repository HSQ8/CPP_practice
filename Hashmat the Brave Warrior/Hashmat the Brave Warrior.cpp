#include <iostream>
#include <string>
#include <array>
#include <vector>
#include <stdio.h>
#include <cmath>
using namespace std;
// uva problem 10055 Hashmat the Brave Warrior
int main(){
 // freopen("in.dat", "r", stdin);
 long int first, second;
  while (cin >> first >> second){
      cout << (long int)abs(second - first) << endl;
     }
return 0;
}
